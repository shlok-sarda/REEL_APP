"""Parallel personalization v2 engine.

This package is intentionally additive. It reads the current extraction output
from `reel_items` and builds a separate structured feature + graph layer
without changing the existing extractor or current library-serving path.
"""

