services:
  - type: web
    name: reel-app
    env: python
    rootDir: final_pipeline
    buildCommand: pip install -r requirements.txt
    startCommand: API_HOST=0.0.0.0 python3 api.py
    autoDeploy: true
